#include "SImpleType.h"



/*
// Compare two itemtypes.
RelationType SimpleType::CompareByNumber(const SimpleType &data)
{
	if (this->s_Number > data.s_Number)
		return GREATER;
	else if (this->s_Number < data.s_Number)
		return LESS;
	else
		return EQUAL;
}
*/
